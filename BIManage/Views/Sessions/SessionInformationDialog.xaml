<Window x:Class="BIManageRevit.BIManage.Views.Sessions.SessionInformationDialog"
        Topmost="False"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BIManageRevit.BIManage.Views.Sessions"
        Title="Session Information"
        Width="900"
        Height="450"
        MinWidth="900"
        MinHeight="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <!-- Tab Button Style -->
        <Style x:Key="TabButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Active Tab Button Style -->
        <Style x:Key="ActiveTabButton" TargetType="Button" BasedOn="{StaticResource TabButton}">
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#FF5500"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Data Grid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E2E8F0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FAFBFC"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#1E293B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Info Card Style -->
        <Style x:Key="InfoCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
        </Style>

        <!-- Label Style -->
        <Style x:Key="InfoLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>

        <!-- Value Style -->
        <Style x:Key="InfoValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <!-- Refresh Icon Button Style -->
        <Style x:Key="RefreshIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Filter Toggle Button Style -->
        <Style x:Key="FilterToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F5F9"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#CBD5E1"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF5500"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF5500"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Filter ComboBox Style -->
        <Style x:Key="FilterComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="6,0"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="White" BorderBrush="#E2E8F0"
                                    BorderThickness="1" CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" Margin="6,0,0,0"
                                                      VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
                                    <Path Grid.Column="1" Data="M0,0 L3,3 L6,0" Stroke="#64748B" StrokeThickness="1"
                                          HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                </Grid>
                            </Border>
                            <ToggleButton x:Name="toggleButton"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1"
                                        CornerRadius="6" Margin="0,2,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="2" Opacity="0.12"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="150">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#CBD5E1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="10" Background="White" CornerRadius="10" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.18"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="#F8FAFC" CornerRadius="10,10,0,0"
                    BorderBrush="#E2E8F0" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="36" Margin="12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="pack://application:,,,/BIManageRevit;component/BIManage/Resources/Icons/BIManage.png"
                               Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                                   FontSize="12" FontWeight="SemiBold" Foreground="#1E293B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                            Click="CloseButton_Click" Width="28" Height="28"
                            BorderThickness="0" Background="Transparent" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="closeBg" Background="Transparent" CornerRadius="6" Width="28" Height="28">
                                    <TextBlock Text="&#x2715;" FontSize="10" Foreground="#94A3B8"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="closeBg" Property="Background" Value="#F1F5F9"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Existing content -->
            <Grid Grid.Row="1" VerticalAlignment="Stretch" Background="#F8FAFC">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Tab Buttons -->
                <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal" Margin="16,0">
                        <Button x:Name="TabSessionInfo" Content="Session Information"
                                Style="{StaticResource ActiveTabButton}" Click="TabSessionInfo_Click"/>
                        <Button x:Name="TabOpenDocuments" Content="Open Documents"
                                Style="{StaticResource TabButton}" Click="TabOpenDocuments_Click"/>
                    </StackPanel>
                </Border>

                <!-- Content Area -->
                <Grid Grid.Row="1" Margin="16,12,16,12">
                    <!-- Tab 1: Session Information -->
                    <Border x:Name="PanelSessionInfo" Visibility="Visible">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <UniformGrid Columns="2" Margin="0">
                                <!-- Row 1: Computer Name | Revit Username -->
                                <Border Style="{StaticResource InfoCard}">
                                    <StackPanel>
                                        <TextBlock Text="Computer Name" Style="{StaticResource InfoLabel}"/>
                                        <TextBlock x:Name="TxtComputerName" Text="-" Style="{StaticResource InfoValue}"/>
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource InfoCard}">
                                    <StackPanel>
                                        <TextBlock Text="Revit Username" Style="{StaticResource InfoLabel}"/>
                                        <TextBlock x:Name="TxtRevitUsername" Text="-" Style="{StaticResource InfoValue}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Row 2: Revit Build | Loaded Plugin Count -->
                                <Border Style="{StaticResource InfoCard}">
                                    <StackPanel>
                                        <TextBlock Text="Revit Build" Style="{StaticResource InfoLabel}"/>
                                        <TextBlock x:Name="TxtRevitBuild" Text="-" Style="{StaticResource InfoValue}"/>
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource InfoCard}">
                                    <StackPanel>
                                        <TextBlock Text="Loaded Plugin Count" Style="{StaticResource InfoLabel}"/>
                                        <TextBlock x:Name="TxtPluginCount" Text="-" Style="{StaticResource InfoValue}"/>
                                    </StackPanel>
                                </Border>

                                <!-- Row 3: Started At | Session Duration -->
                                <Border Style="{StaticResource InfoCard}">
                                    <StackPanel>
                                        <TextBlock Text="Started At" Style="{StaticResource InfoLabel}"/>
                                        <TextBlock x:Name="TxtStartedAt" Text="-" Style="{StaticResource InfoValue}"/>
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource InfoCard}">
                                    <Grid>
                                        <StackPanel>
                                            <TextBlock Text="Session Duration" Style="{StaticResource InfoLabel}"/>
                                            <TextBlock x:Name="TxtSessionDuration" Text="-" Style="{StaticResource InfoValue}"/>
                                        </StackPanel>
                                        <Button Style="{StaticResource RefreshIconButton}"
                                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                                Click="Refresh_Click" ToolTip="Refresh">
                                            <Path Data="M17.65 6.35A7.958 7.958 0 0012 4a8 8 0 108 8h-2a6 6 0 11-1.76-4.24l-1.41 1.41L12 6.34V4z"
                                                  Fill="#64748B" Width="12" Height="12" Stretch="Uniform"/>
                                        </Button>
                                    </Grid>
                                </Border>

                                <!-- Row 4: Opening Duration | Journal File -->
                                <Border Style="{StaticResource InfoCard}">
                                    <StackPanel>
                                        <TextBlock Text="Opening Duration" Style="{StaticResource InfoLabel}"/>
                                        <TextBlock x:Name="TxtOpeningDuration" Text="-" Style="{StaticResource InfoValue}"/>
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource InfoCard}">
                                    <StackPanel>
                                        <TextBlock Text="Journal File" Style="{StaticResource InfoLabel}"/>
                                        <TextBlock x:Name="TxtJournalFileName" Text="-" Style="{StaticResource InfoValue}"/>
                                    </StackPanel>
                                </Border>

                            </UniformGrid>
                        </ScrollViewer>
                    </Border>

                    <!-- Tab 2: Open Documents -->
                    <Border x:Name="PanelOpenDocuments" Visibility="Collapsed" Background="White"
                            BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Filter Bar -->
                            <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="10,6">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Filter:" FontSize="10" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <ToggleButton x:Name="FilterFamily" Content="Family Document"
                                                  Style="{StaticResource FilterToggleButton}"
                                                  Checked="FilterToggle_Changed" Unchecked="FilterToggle_Changed"/>
                                    <ToggleButton x:Name="FilterWorkshared" Content="Workshared"
                                                  Style="{StaticResource FilterToggleButton}"
                                                  Checked="FilterToggle_Changed" Unchecked="FilterToggle_Changed"/>
                                    <ToggleButton x:Name="FilterCloud" Content="Cloud Model"
                                                  Style="{StaticResource FilterToggleButton}"
                                                  Checked="FilterToggle_Changed" Unchecked="FilterToggle_Changed"/>
                                </StackPanel>
                            </Border>

                            <DataGrid x:Name="GridOpenDocuments" Grid.Row="1" Margin="0"
                                      HorizontalScrollBarVisibility="Disabled">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="S.No" Binding="{Binding RowNumber}" Width="50"/>
                                    <DataGridTextColumn Header="Document Name" Binding="{Binding DocumentTitle}" Width="*"/>
                                    <DataGridTextColumn Header="Project" Binding="{Binding ProjectName}" Width="1.5*"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <!-- Empty State -->
                            <Border x:Name="EmptyDocuments" Grid.Row="1" Background="#F8FAFC" Visibility="Collapsed">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="No Open Documents" FontSize="14" FontWeight="Medium"
                                               Foreground="#64748B" HorizontalAlignment="Center"/>
                                    <TextBlock Text="No documents are currently open in this session" FontSize="11"
                                               Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>

                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>

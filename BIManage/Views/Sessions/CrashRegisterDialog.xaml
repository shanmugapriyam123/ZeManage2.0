<Window x:Class="BIManageRevit.BIManage.Views.Sessions.CrashRegisterDialog"

        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crash Register"
        Width="720"
        Height="540"
        MinWidth="650"
        MinHeight="540"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        KeyDown="Window_KeyDown">
    <Window.Icon>
        <BitmapImage UriSource="pack://application:,,,/BIManageRevit;component/BIManage/Resources/Icons/BIManage.png"/>
    </Window.Icon>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/BIManageRevit;component/BIManage/Views/styles/DialogStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Data Grid Style -->
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="#E2E8F0"/>
                <Setter Property="RowBackground" Value="White"/>
                <Setter Property="AlternatingRowBackground" Value="#FAFBFC"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="SelectionMode" Value="Single"/>
                <Setter Property="SelectionUnit" Value="FullRow"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="CanUserReorderColumns" Value="False"/>
                <Setter Property="CanUserResizeRows" Value="False"/>
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="IsReadOnly" Value="True"/>
            </Style>

            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#F8FAFC"/>
                <Setter Property="Foreground" Value="#475569"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="BorderBrush" Value="#E2E8F0"/>
            </Style>

            <Style TargetType="DataGridCell">
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8,5"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Foreground" Value="#1E293B"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#EFF6FF"/>
                        <Setter Property="Foreground" Value="#1E293B"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Analysis Button Style -->
            <Style x:Key="AnalysisButton" TargetType="Button">
                <Setter Property="Background" Value="#FF5500"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Padding" Value="10,4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF5500"/>
                                    <Setter TargetName="border" Property="TextElement.Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF5500"/>
                                    <Setter TargetName="border" Property="TextElement.Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Margin="10" Background="White" CornerRadius="10" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.18"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#F8FAFC" CornerRadius="10,10,0,0"
                    BorderBrush="#E2E8F0" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="36" Margin="12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="pack://application:,,,/BIManageRevit;component/BIManage/Resources/Icons/BIManage.png"
                               Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                                   FontSize="12" FontWeight="SemiBold" Foreground="#1E293B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                            Click="CloseButton_Click" Width="28" Height="28"
                            BorderThickness="0" Background="Transparent" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="closeBg" Background="Transparent" CornerRadius="6" Width="28" Height="28">
                                    <TextBlock Text="&#x2715;" FontSize="10" Foreground="#94A3B8"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="closeBg" Property="Background" Value="#F1F5F9"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        <Grid Grid.Row="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Background="#FFF3E0" Width="34" Height="34" CornerRadius="17" Margin="0,0,10,0">
                        <TextBlock Text="&#x26A0;" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Crash Register"
                                   FontSize="15" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="Previous session crash history"
                                   FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Status Filter - Tab Style -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" x:Name="FilterTabs">
                    <Border x:Name="TabCrashed" Background="#FF5500" CornerRadius="6" Padding="12,6" Margin="0,0,6,0" Cursor="Hand"
                            MouseDown="FilterTab_Click" Tag="Crashed">
                        <TextBlock Text="Crashed" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                    </Border>
                    <Border x:Name="TabActive" Background="Transparent" CornerRadius="6" Padding="12,6" Margin="0,0,6,0" Cursor="Hand"
                            MouseDown="FilterTab_Click" Tag="Active">
                        <TextBlock Text="Active" FontSize="11" FontWeight="Medium" Foreground="#64748B"/>
                    </Border>
                    <Border x:Name="TabClosed" Background="Transparent" CornerRadius="6" Padding="12,6" Margin="0,0,6,0" Cursor="Hand"
                            MouseDown="FilterTab_Click" Tag="Closed">
                        <TextBlock Text="Closed" FontSize="11" FontWeight="Medium" Foreground="#64748B"/>
                    </Border>
                    <Border x:Name="TabAll" Background="Transparent" CornerRadius="6" Padding="12,6" Cursor="Hand"
                            MouseDown="FilterTab_Click" Tag="All">
                        <TextBlock Text="All" FontSize="11" FontWeight="Medium" Foreground="#64748B"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="16,12,16,12">
            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <DataGrid x:Name="GridCrashedSessions" Margin="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="S.No" Binding="{Binding RowNumber}" Width="50"/>
                            <DataGridTextColumn Header="Journal" Binding="{Binding JournalFileNameShort}" Width="1.3*"/>
                            <DataGridTextColumn Header="Started At" Binding="{Binding StartedAtLocal}" Width="1.8*"/>
                            <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="1.2*"/>
                            <DataGridTemplateColumn Header="Status" Width="0.9*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border x:Name="StatusBadge" CornerRadius="6" Padding="8,3" HorizontalAlignment="Left">
                                            <TextBlock x:Name="StatusText" Text="{Binding Status}" FontSize="10" FontWeight="SemiBold"/>
                                        </Border>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Crashed">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#FEE2E2"/>
                                                <Setter TargetName="StatusText" Property="Foreground" Value="#DC2626"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Active">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#DCFCE7"/>
                                                <Setter TargetName="StatusText" Property="Foreground" Value="#16A34A"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Closed">
                                                <Setter TargetName="StatusBadge" Property="Background" Value="#E0E7FF"/>
                                                <Setter TargetName="StatusText" Property="Foreground" Value="#4F46E5"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Action" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Analyze" Style="{StaticResource AnalysisButton}"
                                                Click="Analysis_Click" Tag="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Empty State -->
                    <Border x:Name="EmptyCrashed" Background="#F8FAFC" Visibility="Collapsed">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="&#x1F50D;" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="No Sessions Found" FontSize="14" FontWeight="Medium"
                                       Foreground="#64748B" HorizontalAlignment="Center"/>
                            <TextBlock Text="No sessions match the selected filter" FontSize="11"
                                       Foreground="#94A3B8" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
        </Grid>
    </Border>
</Window>

<Window x:Class="BIManageRevit.BIManage.Views.ModelRegistrationDialog"
        Topmost="False"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Model Registration"
        MinWidth="400"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        KeyDown="Window_KeyDown">
    <Window.Icon>
        <BitmapImage UriSource="pack://application:,,,/BIManageRevit;component/BIManage/Resources/Icons/BIManage.png"/>
    </Window.Icon>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/BIManageRevit;component/BIManage/Views/styles/DialogStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Margin="10" Background="White" CornerRadius="10" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.18"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Standard title bar -->
            <Border Grid.Row="0" Background="#F8FAFC" CornerRadius="10,10,0,0"
                    BorderBrush="#E2E8F0" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="36" Margin="12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="pack://application:,,,/BIManageRevit;component/BIManage/Resources/Icons/BIManage.png"
                               Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                                   FontSize="12" FontWeight="SemiBold" Foreground="#1E293B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                            Click="CloseButton_Click" Width="28" Height="28"
                            BorderThickness="0" Background="Transparent" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="closeBg" Background="Transparent" CornerRadius="6" Width="28" Height="28">
                                    <TextBlock Text="&#x2715;" FontSize="10" Foreground="#94A3B8"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="closeBg" Property="Background" Value="#F1F5F9"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Existing content -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <Border Width="34" Height="34" CornerRadius="17" Margin="0,0,10,0" Background="#FF5500">
                        <TextBlock x:Name="HeaderIcon" Text="&#x2705;" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock x:Name="TxtTitle" Text="Model Registered"
                                   FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock x:Name="TxtSubtitle" Text="Registration successful"
                                   FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Content -->
                <StackPanel Grid.Row="1" Margin="0,0,0,14">
                    <!-- Model Details Card -->
                    <Border x:Name="ModelDetailsCard" Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                            CornerRadius="6" Padding="10,8" Margin="0,0,0,10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Model Name -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Model" FontSize="10" Foreground="#64748B"
                                       VerticalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock x:Name="TxtModelName" Grid.Row="0" Grid.Column="1"
                                       FontSize="10" FontWeight="SemiBold" Foreground="#1E293B"
                                       VerticalAlignment="Center" Margin="0,0,0,5"/>

                            <Border Grid.Row="1" Grid.ColumnSpan="2" Height="1" Background="#F1F5F9" Margin="0,0,0,5"/>

                            <!-- Model ID -->
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Model ID" FontSize="10" Foreground="#64748B"
                                       VerticalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock x:Name="TxtModelId" Grid.Row="2" Grid.Column="1"
                                       FontSize="9" FontWeight="SemiBold" Foreground="#1E293B"
                                       VerticalAlignment="Center" Margin="0,0,0,5"/>

                            <Border Grid.Row="3" Grid.ColumnSpan="2" Height="1" Background="#F1F5F9" Margin="0,0,0,5"/>

                            <!-- Type -->
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Type" FontSize="10" Foreground="#64748B"
                                       VerticalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock x:Name="TxtModelType" Grid.Row="4" Grid.Column="1"
                                       FontSize="10" FontWeight="SemiBold" Foreground="#1E293B"
                                       VerticalAlignment="Center" Margin="0,0,0,5"/>

                            <Border Grid.Row="5" Grid.ColumnSpan="2" Height="1" Background="#F1F5F9" Margin="0,0,0,5"
                                    x:Name="ProjectSeparator"/>

                            <!-- Project -->
                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Project" FontSize="10" Foreground="#64748B"
                                       VerticalAlignment="Center" x:Name="ProjectLabel"/>
                            <TextBlock x:Name="TxtProject" Grid.Row="6" Grid.Column="1"
                                       FontSize="10" FontWeight="SemiBold" Foreground="#1E293B"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Status Message -->
                    <Border x:Name="StatusBorder" CornerRadius="6" Padding="10,8" Margin="0,0,0,10">
                        <TextBlock x:Name="TxtStatus" FontSize="10" TextWrapping="Wrap"/>
                    </Border>

                    <!-- Features Card (only for new registration) -->
                    <Border x:Name="FeaturesSection" Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                            CornerRadius="6" Padding="10,8" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="Features enabled:" FontSize="10" FontWeight="SemiBold"
                                       Foreground="#1E293B" Margin="0,0,0,6"/>
                            <StackPanel x:Name="FeaturesList"/>
                            <TextBlock Text="These settings can be customized by your administrator."
                                       FontSize="9" Foreground="#94A3B8" FontStyle="Italic" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Close Button -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="1" Content="Close" Click="Close_Click"
                            Style="{StaticResource PrimaryButton}" Width="80"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>

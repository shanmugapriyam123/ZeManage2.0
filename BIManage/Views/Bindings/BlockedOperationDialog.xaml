<Window x:Class="BIManageRevit.BIManage.Views.Bindings.BlockedOperationDialog"
        Topmost="False"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pin Protection"
        Width="480"
        Height="Auto"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/BIManageRevit;component/BIManage/Views/styles/DialogStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Margin="10" Background="White" CornerRadius="10" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.18"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="#F8FAFC" CornerRadius="10,10,0,0"
                    BorderBrush="#E2E8F0" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="36" Margin="12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="pack://application:,,,/BIManageRevit;component/BIManage/Resources/Icons/BIManage.png"
                               Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                                   FontSize="12" FontWeight="SemiBold" Foreground="#1E293B" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                            Click="CloseButton_Click" Width="28" Height="28"
                            BorderThickness="0" Background="Transparent" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="closeBg" Background="Transparent" CornerRadius="6" Width="28" Height="28">
                                    <TextBlock Text="&#x2715;" FontSize="10" Foreground="#94A3B8"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="closeBg" Property="Background" Value="#F1F5F9"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Existing content -->
            <Grid Grid.Row="1" Margin="16,14,16,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title with Icon -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <Border Background="#FFF3E0" Width="36" Height="36" CornerRadius="18" Margin="0,0,10,0">
                        <TextBlock Text="🔒" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Protected Element - Operation Blocked"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Content -->
                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <!-- Alert Banner -->
                    <Border Background="#FFF3E0" BorderBrush="#FF9800" BorderThickness="1"
                            CornerRadius="6" Padding="8" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠️" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Style="{StaticResource DialogInstruction}" Margin="0">
                                <Run Text="Cannot " FontWeight="SemiBold"/>
                                <Run Text="{Binding Mode, Mode=OneWay}" FontWeight="Bold" Foreground="#E65100"/>
                                <Run Text=" " FontWeight="SemiBold"/>
                                <Run Text="{Binding ElementCount, Mode=OneWay}" FontWeight="Bold" Foreground="#E65100"/>
                                <Run Text=" protected element(s)" FontWeight="SemiBold"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Element List -->
                    <Border Background="White" CornerRadius="6" Padding="10,8" Margin="0,0,0,8"
                            BorderBrush="#E2E8F0" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Protected Elements" FontSize="9" FontWeight="SemiBold"
                                       Foreground="#94A3B8" Margin="0,0,0,3"/>
                            <ScrollViewer MaxHeight="100" VerticalScrollBarVisibility="Auto">
                                <TextBlock Text="{Binding ElementNames, Mode=OneWay}"
                                           FontFamily="Consolas" FontSize="10"
                                           Foreground="#334155" TextWrapping="Wrap"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Protected Pin Mode Info -->
                    <Border Background="#FFF3E0" BorderBrush="#FFB74D" BorderThickness="1"
                            CornerRadius="6" Padding="8">
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold" Foreground="#E65100" FontSize="11">
                                Protected Pin Mode: <Run Text="{Binding Mode, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock FontSize="10" Foreground="#5D4037" Margin="0,3,0,0" TextWrapping="Wrap" Text=""/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- OK Button -->
                <StackPanel Grid.Row="2" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>
